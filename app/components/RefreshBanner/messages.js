/*
 * RefreshBanner Messages
 *
 * This contains all the text for the RefreshBanner component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  notification: {
    id: 'app.components.RefreshBanner.notification',
    defaultMessage: 'There is a new version available! Click to refresh.',
  },
});
