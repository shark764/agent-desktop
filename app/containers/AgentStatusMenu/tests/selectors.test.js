// import { selectAgentStatusMenuDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'expect';

// const selector = selectAgentStatusMenuDomain();

describe('selectAgentStatusMenuDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
