/*
 * PhoneControlsInactive Messages
 *
 * This contains all the text for the PhoneControlsInactive component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  call: {
    id: 'app.containers.PhoneControls.call',
    defaultMessage: 'Call',
  },
});
